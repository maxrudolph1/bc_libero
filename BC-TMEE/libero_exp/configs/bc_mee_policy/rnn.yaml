defaults:
  - _self_
  - ../base/data@data: default
  - ../base/algo@algo: bc_mee_policy
  - ../base/policy@policy: rnn_policy    # policy_name: BCRNNPolicy, image encoder: resnet, fuse: rnn
  - ../base/train@train: default
  - ../base/eval@eval: default
  - ../base/env@env: default


experiment_dir: ???

hydra:
  run:
    dir: ./outputs/libero/${algo.algo_name}/imbalance/rnn/${data.env_name}/ratio${data.train_ratio}/sigma${train.sigma}/mee_weight${train.mee_weight}/${now:%m%d}_${now:%H%M}_seed${train.seed}
  sweep:
    dir: ./outputs/libero/${algo.algo_name}/imbalance/rnn/${data.env_name}/ratio${data.train_ratio}/sigma${train.sigma}/mee_weight${train.mee_weight}/${now:%m%d}_${now:%H%M}_seed${train.seed}
    subdir: ${hydra.job.num}

wandb:
  use_wandb: true
  project: bc-ib-rnn
  name: ${data.env_name}_${algo.algo_name}_rnn_sigma${train.sigma}_mee_weight${train.mee_weight}_${now:%m%d}_${now:%H%M}_seed${train.seed}_ratio${data.train_ratio}_${hydra:job.num}
  group: ${data.env_name}_${algo.algo_name}_sigma${train.sigma}_mee_weight${train.mee_weight}_rnn
  dir: ./wandb/${env.env_type}/${algo.algo_name}/sigma${train.sigma}/mee_weight${train.mee_weight}/rnn/${data.env_name}/ratio${data.train_ratio}

