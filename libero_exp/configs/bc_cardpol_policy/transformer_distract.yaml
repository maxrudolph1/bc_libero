defaults:
  - transformer
  - override ../base/data@data: distract
  - _self_
  - ../base/output@_global_: run_dir

eval:
  post_train_rollout:
    enable: true
    eval_all: false
    log_video: true


wandb:
  group: ${data.env_name}_${algo.algo_name}_transformer_distract
